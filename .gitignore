# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/
env/

# Runtime data (gitignored by design — see foundation/config.py)
data/
PAUSE

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Env files
.env
.env.*
!.env.example

experiments/exp001-out/
experiments/exp001-tier1-pilot/

# Worktrees created inside the checkout are NOT content -- a bare `git add -A`
# records one as a broken submodule pointer (gitlink), which no clone can
# resolve. Happened 2026-08-29 when a parallel session made one here.
.claude/worktrees/
